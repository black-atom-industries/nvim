return {
    config = require("terra-core.actions.config"),
    color = require("terra-core.actions.color"),
    ui = require("terra-core.actions.ui"),
    highlights = require("terra-core.actions.highlights"),
    files = require("terra-core.actions.files"),
    themes = require("terra-core.actions.themes"),
}
