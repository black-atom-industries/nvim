local lib = {
    config = require("terra-core.lib.config"),
    color = require("terra-core.lib.color"),
    ui = require("terra-core.lib.ui"),
    hls = require("terra-core.lib.highlights"),
    files = require("terra-core.lib.files"),
    themes = require("terra-core.lib.themes"),
    debug = require("terra-core.lib.debug"),
    bg = require("terra-core.lib.bg"),
}

return lib
