local utils = {
    config = require("terra-core.utils.config"),
    color = require("terra-core.utils.color"),
    ui = require("terra-core.utils.ui"),
    hls = require("terra-core.utils.highlights"),
    files = require("terra-core.utils.files"),
    themes = require("terra-core.utils.themes"),
    debug = require("terra-core.utils.debug"),
    bg = require("terra-core.utils.background"),
}

return utils
