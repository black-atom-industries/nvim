return {
	config = require("terra.actions.config"),
	color = require("terra.actions.color"),
	ui = require("terra.actions.ui"),
	utils = require("terra.actions.utils"),
	highlights = require("terra.actions.highlights"),
}
